package com.company;

public class Strength extends Person {

/*    private Person strength = new Person(100, 2, 3, 4, 0, 1, 5);
    private Monsters m1l1 = new Monsters(10, 1, 0, 1);
    int x, xplvl = 0;

    // Метод ближнего боя
    public int figt() {
        x = m1l1.heals % strength.fight;
        if (x == 0) {
            strength.heals = strength.heals - ((m1l1.heals / strength.fight) * m1l1.fight);
            if (strength.heals > 0) {
                xplvl += m1l1.lvl;
            }
        }
        if (x > 0 && x < 1) {
            strength.heals = strength.heals - (m1l1.fight * (m1l1.heals / strength.fight)) - m1l1.fight;
            if (strength.heals > 0) {
                xplvl++;
            }
        }
        return strength.heals;
    }

    // Метод выстрела
    public int shot() {
        x = (m1l1.heals - strength.shot * 2) % strength.fight;
        if (x == 0) {
            strength.heals = strength.heals - ((m1l1.heals - strength.shot * 2) / m1l1.fight) * m1l1.fight;
            strength.patron -= 2;
            if (strength.heals > 0) {
                xplvl++;
            }
        }
        if (x > 0 && x < 1) {
            strength.heals = strength.heals - ((m1l1.heals - strength.shot * 2) / m1l1.fight) * m1l1.fight - m1l1.fight;
            strength.patron -= 2;
            if (strength.heals > 0) {
                xplvl++;
            }
        }
        return strength.heals;
    }*/

}
